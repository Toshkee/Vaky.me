# Fixer report — telo-pilates (all 10 plan changes claimed done)

1. **Signature mark rebuilt** — filled reformer side elevation, viewBox 0 0 900 200, no preserveAspectRatio; two 18-unit rails, footbar frame left, taller riser right, 180×42 carriage deck with shoulder-rest block, all currentColor fills. Deleted .trackEcho/.trackTick/.trackStroke/.trackEnd + stroke-width ladder + both old keyframes; one .carriage rule translates the deck 540 units on animation-timeline --telo-carriage, settled position is static default. Used twice (hero, close). Fixer rasterized at 1184px/370px and iterated geometry twice.
2. **Apparatus promoted, language codes demoted** — ink band now: claim → five <li> apparatus at clamp(1.5rem,4.5vw,2.25rem) bold butter in border-y flex-wrap set → language sentence at body scale with codes inline. join(" · ") gone.
3. **Formats board** — every row an <a> to bookingUrl (_blank, noopener, data-umami-event-action="booking-format-<id>") with ink rule scaling from origin-left on hover/focus-visible + "otvori booking ↗" label. ENTRY_FORMAT (grupni-reformer) full-width at clamp(2.5rem,6vw,4rem); other four at clamp(1.9rem,4vw,2.75rem). DEVIATIONS: rule is ink not butter (butter on chalk ~1.05:1 invisible); columns [minmax(0,23rem)_minmax(0,26rem)] gap-x-12 instead of max-content_32rem (per-row max-content breaks board alignment).
4. **Phone formats re-authored** — md:hidden 2-col <details> tap grid, border-l-2/t-2 container + border-b-2/r-2 cells, min-h-28, group-open:rotate-45 +, open:bg butter (only butter on light half), entry format spans both columns. Desktop <ul> hidden md:block.
5. **Ghost numerals deleted, chalk sections merged** — 1/2/3 gone, <ol>→<ul> plain items; first-visit + location = one chalk band, lg:grid-cols-[1.1fr_0.9fr] items-start. Band sequence ink→chalk→ink→chalk→ink.
6. **font-black monopoly broken** — 900 only on h1, formats h2, close h2; everything else 700/600/500/400. Terminal full stop retired from 3 display lines, kept on wordmark-echo lines (h1, "telo u pokretu.", "telo pilates club.").
7. **Hero h1** — now "reformer. / pet / formata." at unchanged clamp; eyebrow deleted; "Reformer pilates · Vektra, Podgorica" sits below h1 under a hairline at 0.95rem.
8. **Location semantics** — "vektra, podgorica" is the h2, GDJE kicker gone (zero kickers remain); Instagram link clamp(1.35rem,4vw,1.9rem) bold with handle written out, min-h-14.
9. **Container break + closing void** — hero mark row has -mr-5 sm:-mr-8 xl:-mr-24 with section pb-0 (machine legs land on ink/chalk seam). Only overhang on page. Closing lg:items-end→items-start + lg:pt-4. DEVIATION: no calc(50%-50vw) (scrollbar overflow risk breaking view-timeline).
10. **Phone CTA + header** — primary w-full sm:w-auto lg:w-full; secondary below hairline mt-7 pt-5; headerCta now visible at all widths (inline-flex min-h-11 px-4 sm:px-6, ~316px of 350px at 390).

Verification claimed: tsc clean, eslint clean, all anchors resolve, no stale CSS/refs. Route 730 lines (was 731).
